:ruby
  data = {
    user:       user_name(notification.subject.author),
    idea_link:  link_to(notification.subject.idea.title, notification.subject.idea)
  }

= _('%{user} submitted %{idea_link}').html_safe % data