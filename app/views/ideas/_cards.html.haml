-# Give me an array of +@ideas+

.row
  .span3
    = render 'filters'
  .span9
    = render 'no_ideas_banner'
    - @ideas.each do |idea|
      = render 'idea', idea:idea, collapsed:true, title_link:true
